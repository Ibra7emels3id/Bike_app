/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["images.unsplash.com", "hbr.org" , "surlybikes.com"],
    },
};

export default nextConfig;
