/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["images.unsplash.com", "assets-160c6.kxcdn.com"],
    },
};

export default nextConfig;
